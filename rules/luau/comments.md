# Comments — Luau
 
Extends `global/comments.md`.
 
**Not comments:** `--!strict`, `--!nonstrict`, `--!native`, `--!nolint` and similar directives.
 
**Doc-comment formats:** if the codebase documents public API with Moonwave (`--[=[ @within ]=]`), match it for public methods instead of a plain `--` line. Its length is set by the format, not by the 2-line limit.